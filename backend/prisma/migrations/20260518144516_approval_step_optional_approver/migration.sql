BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[approval_steps] ALTER COLUMN [approverId] UNIQUEIDENTIFIER NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
