/**
 * @deprecated O modo demo agora roda 100% no frontend. Use o catálogo
 * estático em `lib/demo/catalog.ts` e o store em `lib/demo/state.ts`.
 *
 * Este arquivo permanece apenas para não quebrar imports legados.
 */
export { DEMO_USERS as useDemoUsers } from './demo/catalog';
